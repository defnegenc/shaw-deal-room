GEMINI_API_KEY=
# Optional. Override if you want a different Gemini Flash model.
GEMINI_MODEL=gemini-2.0-flash

# Optional. If set, the web research tool calls Serper. If unset, it uses
# deterministic mocked web-search results for the demo companies.
SERPER_API_KEY=
